# Cursor — mở project thế nào

1. Workspace = thư mục **`pdf_splitter`** (chứa `main.py`, `AGENTS.md`).
2. Chat mới → mở [`docs/CURSOR_HANDOFF.md`](docs/CURSOR_HANDOFF.md) → copy khối trong fence → dán gửi.
3. Hoặc gõ `@AGENTS.md` rồi: *Đã đọc AGENTS. Chờ tôi giao việc.*

Rule tự áp: `.cursor/rules/hsdv-splitter.mdc` (`alwaysApply: true`).
