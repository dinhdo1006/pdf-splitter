pymupdf>=1.24.0
# Paddle GPU: KHÔNG cài `paddlepaddle` (CPU) nếu muốn dùng GPU.
# RTX 30/40:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# RTX 50 (sm_120, Blackwell — 5060 Ti/5070/5080/5090):
#   pip uninstall -y paddlepaddle paddlepaddle-gpu
#   pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#   pip install --force-reinstall --no-deps nvidia-nccl-cu13==2.29.7  # nếu venv còn PyTorch
# CPU only:   pip install paddlepaddle==3.3.0
paddleocr>=3.0.0
opencv-python>=4.9.0
numpy>=1.26.0
rapidfuzz>=3.6.0
unidecode>=1.3.8
loguru>=0.7.2
tqdm>=4.66.0

# MinIO luồng inbox → pipeline → output
minio>=7.2.0
python-dotenv>=1.0.0

# Handwriting OCR (optional — comment out nếu không cần)
transformers>=4.40.0
torch>=2.2.0
Pillow>=10.0.0
sentencepiece>=0.2.0
